package arida.ufc.br.moap.stop.spi;

import arida.ufc.br.moap.core.spi.IAnnotable;
import org.joda.time.Interval;



/**
 *
 * @author igobrilhante
 * @param <S>
 * 
 * <p>
 * Interface to define a general Stop where the extent (spatial dimension) is instance of S.
 * S if the representation of the <b>stop</p>, and it is usually represented as a single point or a list of points
 * </p>
 */
public interface IStop<S> extends IAnnotable {
	/**
     *
     * @return S
     */
    public S getExtent();

}
